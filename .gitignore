# Secrets — never commit
.env

# Local working notes — not for the repo history
implementation_plan.md

# Runtime work directories — large PDFs, logs, output files
remediation_work/

# Python bytecode and build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Status CSVs contain local file paths — generate on each machine
remediation_status.csv

# Generated by `manage.py check_ocr_regression` — reviewable HTML diff reports, one run per
# timestamped subfolder, not committed fixtures. The baseline JSON it diffs against lives in
# tests/fixtures/ and IS committed.
remediation/tests/diff_history/

# Virtual environments
venv/
.venv/
env/

# Lint/type-check caches
.mypy_cache/
.ruff_cache/

# Django
db.sqlite3
db.sqlite3-journal
local_settings.py
staticfiles/
media/

# OS junk
.DS_Store
Thumbs.db
